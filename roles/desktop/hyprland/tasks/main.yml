---
# Desktop Hyprland role - core Hyprland window manager
# This role manages the essential Hyprland components

- name: Install and configure Hyprland window manager
  ansible.builtin.include_tasks: hyprland.yml
  tags: [ desktop ]

- name: Install Hyprland plugins
  ansible.builtin.include_tasks: hyprland-plugins.yml
  tags: [ desktop ]

- name: Install Hyprland tools and utilities
  ansible.builtin.include_tasks: hyprland-tools.yml
  tags: [ desktop ]