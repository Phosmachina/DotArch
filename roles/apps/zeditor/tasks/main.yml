---
- name: Install Zed Editor (Arch)
  ansible.builtin.shell: yay -Sy --noconfirm zed  # noqa: command-instead-of-shell
  become: true
  become_user: '{{ ansible_user }}'
  changed_when: true
  when: ansible_facts.distribution == 'Archlinux'
  tags: [apps, zeditor]
