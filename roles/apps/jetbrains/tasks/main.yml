---
- name: Install JetBrains Toolbox (Arch)
  ansible.builtin.shell: yay -Sy --noconfirm jetbrains-toolbox  # noqa: command-instead-of-shell
  become: true
  become_user: '{{ ansible_user }}'
  changed_when: true
  when: ansible_facts.distribution == 'Archlinux'
  tags: [apps, jetbrains]
