---
# Keyboard configuration

- name: Override us with us_custom keymap for Wayland
  ansible.builtin.copy:
    src: '{{ ansible_facts["env"].HOME }}/.config/keymap/us_custom'
    dest: '/usr/share/xkb/symbols/us'
    remote_src: true
    mode: '0644'
  become: true
  tags: [system, keyboard]
